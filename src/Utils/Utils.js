// export const filterArray = (cardList, selection) => {
//   const filteredArray = cardList.filter((data) => {
//     if (data.type === selection) return true;
//   });
//   return filteredArray;
// };

// import { cardsData } from "./Data";

// export const filterArray = (type, naming) => {
//   const filteredArray = type
//     .filter((data) => {
//       if (data !== "todos") {
//         return data.type === type;
//       }
//       return data;
//     })
//   return filteredArray;
// };
